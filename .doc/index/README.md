# index/ — indici e scorciatoie

Questa cartella può ospitare indici “umani” (non necessari al funzionamento di GUARDIAN)
per navigare rapidamente la libreria, ad esempio:
- TOC dei canonici progetto
- quicklinks a runner e comandi
- mappe UI

Nota: l’indice principale e il fingerprint rimangono in `../CANONICAL_LIBRARY.md`.
